node_modules/
dist/
.env
.env.*
!.env.example
public/brand/
sandbox/*
!sandbox/botleaf-15min/
.DS_Store
.astro/
# Architect-shipped reference HTML — kept locally for layout audits,
# never committed. Drop here so `git add -A` doesn't sweep it up.
v02-comparisons.html
