---
// Page-level masthead — the in-content crumb row above the tab rail.
// NOT the global app masthead (that's Nav.astro's `.nav-masthead` with
// the ticker marquee).
//
// Crumb format: `/SECTORS/{SECTOR}/{ASSET}` — no active-tab suffix
// (the tab rail directly below shows the active tab, suffix would be
// redundant). Spec: ASSETPAGE_BUILD_SPEC.md → Slash-prefix Vernacular.

export interface Props {
  sectorName: string;
  fullName: string;
  stamp: string;       // e.g. '24 APR · 18:55 UTC · COINBASE · BTC-USD'
}

const { sectorName, fullName, stamp } = Astro.props;
---

<div class="crumb-row">
  <p class="crumb">
    <span class="seg">/SECTORS</span>
    <span class="sep">/</span>
    <span class="seg">{sectorName.toUpperCase()}</span>
    <span class="sep">/</span>
    <span class="seg">{fullName.toUpperCase()}</span>
  </p>
  <p class="stamp">{stamp}</p>
</div>

<style>
  .crumb-row {
    display: flex; justify-content: space-between; align-items: baseline;
    padding-bottom: 14px; margin: 0 0 18px;
    border-bottom: 1px solid var(--ink-faint);
    flex-wrap: wrap; gap: 12px 24px;
  }
  .crumb {
    font-family: var(--font-mono); font-size: 10px;
    letter-spacing: 0.22em; text-transform: uppercase;
    /* a11y: promoted from --ink-mid for sub-18px contrast on
       --paper-deep (the binder bg behind the masthead). */
    color: var(--ink-soft); margin: 0;
    display: inline-flex; align-items: baseline; gap: 0;
  }
  .crumb .seg { display: inline; }
  /* `.sep` (slash separators) stays --ink-faint — purely decorative
     punctuation between segments. */
  .crumb .sep { color: var(--ink-faint); }
  .stamp {
    font-family: var(--font-mono); font-size: 10px;
    letter-spacing: 0.22em; text-transform: uppercase;
    /* a11y: promoted from --ink-pale for sub-18px contrast on
       --paper-deep (the binder bg behind the masthead). */
    color: var(--ink-soft); margin: 0;
    font-variant-numeric: tabular-nums;
  }

  @media (max-width: 700px) {
    .crumb { font-size: 9px; letter-spacing: 0.18em; }
    .stamp { font-size: 9px; letter-spacing: 0.18em; }
  }
</style>
