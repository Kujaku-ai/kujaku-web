---
import type { AssetData } from '~/lib/types/asset';

export interface Props { data: AssetData }
const { data } = Astro.props;
---

<section class="phase1-stub">
  <p class="eyebrow">phase 5 · news</p>
  <h2 class="title">News — {data.fullName}</h2>
  <p class="body">
    Two stacked sections (primaries · 主要 + features · 特集), each with
    its own sort-button strip (most recent / popular / liked) and modular
    grid of article + feature tiles. Sources mocked until scraper service
    ships. {data.news.primaries.length} primaries · {data.news.features.length} features queued.
  </p>
</section>

<style>
  .phase1-stub {
    padding: 36px 32px 40px;
    background: var(--paper);
    border-radius: 4px;
    box-shadow:
      inset 0 1px 0 rgba(255,251,240,0.7),
      0 1px 0 rgba(255,251,240,0.5),
      0 4px 12px rgba(40,30,18,0.05);
  }
  .eyebrow {
    font-family: var(--font-mono); font-size: 10px; font-weight: 500;
    letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--red); margin: 0 0 14px;
  }
  .title {
    font-family: var(--font-display); font-style: italic; font-weight: 400;
    font-size: 28px; line-height: 1.1; letter-spacing: -0.01em;
    color: var(--ink); margin: 0 0 14px;
  }
  .body {
    font-family: var(--font-body); font-size: 14px; line-height: 1.6;
    color: var(--ink-mid); margin: 0; max-width: 64ch;
  }
</style>
